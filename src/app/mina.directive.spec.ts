import { MinaDirective } from './mina.directive';

describe('MinaDirective', () => {
  it('should create an instance', () => {
    const directive = new MinaDirective();
    expect(directive).toBeTruthy();
  });
});

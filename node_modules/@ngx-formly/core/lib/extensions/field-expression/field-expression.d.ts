import { FormlyFieldConfigCache } from '../../components/formly.field.config';
import { FormlyExtension } from '../../services/formly.config';
export declare class FieldExpressionExtension implements FormlyExtension {
    onPopulate(field: FormlyFieldConfigCache): void;
    private _evalExpression(expression, parentExpression?);
    private _checkField(field);
    private checkFieldExpressionChange(field);
    private checkFieldVisibilityChange(field);
    private addFieldControl(parent, field);
    private fieldParentFormControl(field);
    private fieldKey(field);
}
